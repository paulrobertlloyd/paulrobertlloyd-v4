/**
 * Returns percentage of two numbers
 * @param {number} total - Total value
 * @param {number} number - This value
 * @returns {number} Percentage
 */
module.exports = (total, number) => (total / number) * 100;
