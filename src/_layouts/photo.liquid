---
layout: default.liquid
---
{% render "footer", properties: properties %}

<flow-content class="e-content p-name" style="--columns: 1" data-font="lede">
  {{ content }}
</flow-content>

{% render "meta", properties: properties %}
