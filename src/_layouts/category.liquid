{%- assign items = collections.public | includes: 'data.category',title -%}
{%- layout 'default' -%}
{%- block 'main' -%}
  <h1 class="main__title">Categories</h1>
  <div class="main__introduction">
    {{ title }}
  </div>
  {%- render 'items' with items
    modifier: 'inline'
  -%}
{%- endblock -%}
