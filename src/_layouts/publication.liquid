---
layout: default.liquid
---
{%- render 'header'
  title: title
  summary: summary
  uid: uid
-%}
{%- assign items = collections.article | where: "data.publication.title", title -%}
{%- render 'items' with items -%}
