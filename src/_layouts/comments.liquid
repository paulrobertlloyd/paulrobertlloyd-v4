---
layout: default.liquid
eleventyComputed:
  title: "{{ comments.size | pluralise: 'comment' }} on ‘{{ title }}’"
---
{%- render 'footer'
  properties: properties
-%}
{%- render 'header'
  properties: properties
-%}
{%- render 'items' with comments
  component: 'comment'
-%}
