---
layout: default
eleventyComputed:
  page_title: "{{ comments.size | pluralise: 'comment' }} on ‘{{ title }}’"
---
{%- render 'footer'
  date: page.date
-%}
{%- render 'header'
  title: page_title
  vocabulary: vocabulary
-%}
{%- render 'items' with comments
  component: 'remark'
-%}
