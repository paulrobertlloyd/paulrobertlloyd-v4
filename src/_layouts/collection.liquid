---
layout: default.liquid
---
{% render "header",
  title: title,
  summary: summary,
  uid: uid
%}

<flow-content class="e-content">
  {{ content }}
</flow-content>

{%- assign collection = collections[page.fileSlug] | reverse -%}
{%- render "section" with collection
  title: "Items in this collection"
  itemsComponent: "card"
-%}
