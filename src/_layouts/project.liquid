---
layout: default.liquid
---
{% render "header",
  title: title,
  summary: summary
%}

<flow-content class="e-content">
  {{ content }}
</flow-content>

{% liquid
  assign related = collections[id]

  if related
    render "section" with related, id: "related", title: "Related", itemsComponent: "card"
  endif
%}
