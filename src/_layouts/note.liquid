{%- capture page_title %}Note: {{ date | date: dates.date_time }} · {{ app.name }}{% endcapture -%}
{%- layout 'default' -%}
{%- block 'main' -%}
  {%- render 'footer'
    date: date
    timeZone: timeZone
    show_time: true
    location: location
    geo: geo
    in_reply_to: in_reply_to
    canonical: canonical
    vocabulary: vocabulary
  -%}
  {%- render 'content' with content
    properties: 'e-content p-name'
  -%}
  {%- render 'meta'
    categories: category
    syndication: syndication
  -%}
{%- endblock -%}
