---
layout: default.liquid
---
{% render "footer", properties: properties %}

{% render "header", properties: properties %}

<flow-content class="e-content">{{ content }}</flow-content>

{% render "meta", properties: properties, commentsMeta: commentsMeta %}
