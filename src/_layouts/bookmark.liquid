---
layout: default.liquid
---
{%- assign comments = collections[id] | find: 'data.type','comments' -%}
{%- render 'footer'
  properties: properties
-%}
{%- render 'header'
  properties: properties
-%}
<flow-content class="e-content">{{ content }}</flow-content>
{%- render 'meta'
  comments: comments
  properties: properties
-%}
