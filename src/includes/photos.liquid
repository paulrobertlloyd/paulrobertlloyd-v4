<figure class="photos" style="--columns: {{ columns | default: photos.size }}">
  {%- render 'image' for photos as image
    class: imageClass
    preset: imagePreset
  -%}
  {%- if caption -%}
    <figcaption class="photos__caption">{{ caption | markdown }}</figcaption>
  {%- endif -%}
</figure>
