<figure class="photos" style="--columns: {{ columns | default: photos.size }}">
{%- for item in photos -%}
  {%- render 'image' with item.url
    alt: item.alt
    class: imageClass
    preset: imagePreset
    ratio: imageRatio
  -%}
{%- endfor -%}
</figure>
