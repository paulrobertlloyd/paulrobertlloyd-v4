---
layout: index
title: Archive
pagination:
  data: months
  size: 1
  alias: page
  addAllPagesToCollections: true
permalink: "{{ page.url }}"
changefreq: monthly
priority: 0.2
eleventyComputed:
  page_title: "{{ page.data.title }} · {{ app.title }}"
  introduction: "{{ page.data.title }}"
---
{%- assign items = collections.post | includes: 'data.date',page.data.date -%}
{%- render 'items' with items
  modifier: 'inline'
  no_items_content: 'Nothing was published during this month.'
-%}
