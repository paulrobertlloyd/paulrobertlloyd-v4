---
title: Writing
summary: Short posts, articles and essays.
introduction: Short posts, articles and essays; mostly written for myself, but sometimes [written for others](/articles/sources/), too.
pagination:
  data: collections.article
  reverse: true
  size: 24
permalink: articles/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber }}{% else %}index{% endif %}.html
---
{%- if pagination.pageNumber < 1 -%}
  {%- assign featured = collections.article | includes: 'data.tags','featured' -%}
  {%- assign selected = featured | reverse %}
  {%- include 'section'
    section_id: 'featured'
    section_title: 'Select articles'
    section_scrollable: true
    items: selected
    items_limit: 6
    items_size: '16rem'
    items_component: 'card'
    card_image_color: true
  -%}
  {%- include 'section'
    section_id: 'recent'
    section_title: 'Recent articles'
    items: pagination.items
    item_modifier: 'inline'
  -%}
{%- else -%}
  {%- include 'items' with pagination.items
    item_modifier: 'inline'
  -%}
{%- endif %}
{%- include 'traverse' label: 'articles' -%}
