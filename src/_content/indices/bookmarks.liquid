---
title: Bookmarks
pagination:
  data: collections.bookmark
  reverse: true
  size: 24
permalink: "bookmarks/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber | plus: 1 }}{% else %}index{% endif %}.html"
changefreq: weekly
---
{%- render 'items' with pagination.items
  item_modifier: 'inline'
-%}
{%- render 'pagination' with pagination -%}
