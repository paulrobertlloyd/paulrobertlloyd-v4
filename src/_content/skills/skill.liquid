---
layout: skill
pagination:
  data: skills
  size: 1
  alias: page
  addAllPagesToCollections: true
permalink: "projects/skills/{{ page.fileSlug }}/"
changefreq: monthly
priority: 0.2
eleventyComputed:
  title: "{{ page.data.title }}"
  page_title: "{{ title }} · {{ app.title }}"
  summary: "{{ page.data.summary }}"
---
