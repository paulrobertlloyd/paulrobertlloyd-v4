---
layout: source
pagination:
  data: sources
  size: 1
  alias: page
  addAllPagesToCollections: true
permalink: "articles/sources/{{ page.fileSlug }}/"
changefreq: monthly
priority: 0.2
eleventyComputed:
  title: "{{ page.data.title }}"
  summary: "{{ page.data.summary }}"
  url: "{{ page.data.url }}"
  tag: "source:{{ page.fileSlug }}"
---
