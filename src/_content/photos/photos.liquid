---
layout: index
title: Photos
pagination:
  data: collections.photo
  reverse: true
  size: 36
permalink: "photos/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber | plus: 1 }}.html{% else %}/{% endif %}"
tags: false
changefreq: daily
priority: 0.9
---
{%- render 'items' with pagination.items
  component: 'card-photo'
  modifier: 'photos'
  grid_size: '16rem'
  image_preset: 'thumbnail'
-%}
{%- render 'traverse' with pagination
  label: 'photos'
-%}
