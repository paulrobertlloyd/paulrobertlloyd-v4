<figure class="align-{{ alignment | default: 'none' }}">
{%- if content %}
  {{ content | markdown }}
{%- else %}
  {%- render 'image' with figure
    preset: image_preset
    ratio: image_ratio | default: false
  -%}
{%- endif %}
{%- if caption %}
  <figcaption>
    {{ caption | markdown }}
  </figcaption>
{%- endif %}
</figure>
