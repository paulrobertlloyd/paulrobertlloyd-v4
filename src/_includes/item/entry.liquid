<article class="item | h-entry" id="{{ item.data.id }}">
{%- if item.data.title -%}
  <header class="item__header">
    <h3 class="item__title | p-name">
      <a class="u-url" rel="bookmark" href="{{ item.url }}">{{ item.data.title | markdown: 'inline' }}</a>
    </h3>
  </header>
{%- endif -%}

{%- if item.data.jam_of -%}
  <div class="item__main s-linear">
    {%- render 'jam' with item.data.jam_of
      content: content
    -%}
  </div>
{%- elsif item.data.summary -%}
  <div class="item__main s-linear | p-summary">
    {{ item.data.summary | markdown }}
  </div>
{%- elsif item.content -%}
  <div class="item__main s-linear | e-content p-name">
    {{ item.content }}
    {% if item.data.photo %}
      {%- render 'gallery' with item.data.photo
        imageClass: 'u-photo'
        imagePreset: 'card-square'
      -%}
    {% endif %}
  </div>
{%- endif -%}

  <footer class="item__footer">
{%- if item.data.title -%}
    {% datetime item.data.published item.data.start item.data.end item.data.time_zone item.data.title %}
{%- else -%}
    <a class="u-url" rel="bookmark" href="{{ item.url }}">
      {% datetime item.data.published item.data.start item.data.end item.data.time_zone %}
    </a>
{%- endif -%}
  </footer>
</article>
