<article class="item | h-entry" data-type="{{ item.data.type }}" style="--item-accent-color: {{ item.data.accent_color }}; --columns: 6">
{%- if item.data.title -%}
  <header>
    <h3 class="p-name">
    {%- if item.data.bookmark_of -%}
      <a class="u-bookmark-of" rel="external" href="{{ item.data.bookmark_of }}">
        {{- item.data.title | markdown: "inline" -}}
      </a>
    {%- else -%}
      <a class="u-url" rel="bookmark" href="{{ item.url }}">
        {{- item.data.title | markdown: "inline" -}}
      </a>
    {%- endif -%}
    </h3>
  </header>
{%- endif -%}

{%- if item.data.summary -%}
  <flow-content>
    <p class="p-summary">{{ item.data.summary | markdown: "inline" }}</p>
  </flow-content>
{%- elsif item.content -%}
  <flow-content class="e-content p-name">
    {{ item.content }}
  </flow-content>
{%- endif -%}

{%- if item.data.date or item.data.start -%}
  <footer>
    <a class="u-url" rel="bookmark" href="{{ item.url }}">
      {%- datetime item.data item.data.title -%}
    </a>
    {%- app_icon item.data.icon item.data.icon_label -%}
  </footer>
{%- endif -%}
</article>
