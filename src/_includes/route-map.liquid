<figure class="align-{{ alignment | default: 'none' }}">
  {% route_map geojson, outputDir, alt %}
{%- if caption %}
  <figcaption>
    {{ caption | markdown }}
  </figcaption>
{%- endif %}
</figure>
