{%- if remarks.size > 0 -%}
  <details class="remarks">
    <summary>
      {% render 'facepile' with remarks
        type: title
      %}
    </summary>

    {%- include 'items' with remarks
      component: 'remark'
      item_modifier: 'inline'
    -%}
  </details>
{%- endif -%}
