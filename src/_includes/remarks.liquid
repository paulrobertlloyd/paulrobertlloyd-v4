{%- if remarks.size > 0 -%}
  <details class="remarks">
    <summary>
      {% render 'facepile' with remarks
        type: title
      %}
    </summary>

    {%- render 'items' with remarks
      component: 'remark'
      modifier: 'remarks'
    -%}
  </details>
{%- endif -%}
