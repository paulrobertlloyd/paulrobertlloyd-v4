<ul class="items" style="--grid-size: {{ gridSize | default: '100%' }}" role="list">
{%- for item in items limit: limit -%}
  <li class="items__item">
    {%- render 'item/{{ component | default: "entry" }}'
      id: forloop.index
      item: item
    -%}
  </li>
{%- else -%}
  <li class="items__item">{{ itemsNoContent | markdown: 'inline' }}</li>
{%- endfor -%}
</ul>
