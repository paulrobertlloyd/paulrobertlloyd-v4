<ul class="items" data-scrollable="{{ scrollable | default: 'false' }}" role="list">
{%- for item in items limit: limit -%}
  <li class="items__item">
    {%- render "item/{{ component | default: 'entry' }}"
      id: forloop.index
      item: item
    -%}
  </li>
{%- endfor -%}
</ul>
