---
layout: index.liquid
title: Categories
pagination:
  data: collections.category
  size: 1
  alias: category
permalink: "categories/{{ category }}/"
eleventyComputed:
  page_title: "{{ category }}"
  introduction: "{{ category }}"
---
{%- assign items = collections.publicVisibility | includes: 'data.category',category -%}
{%- render 'items' with items -%}
