---
layout: index
title: Archive
pagination:
  data: archives.months
  size: 1
  alias: page
  addAllPagesToCollections: true
permalink: "{{ page.url }}"
eleventyComputed:
  changefreq: monthly
  priority: 0.2
  page_title: "{{ page.data.introduction }} · {{ app.name }}"
  introduction: "{{ page.data.introduction }}"
---
{%- assign items = collections.public | includes: 'date',page.data.date -%}
{%- render 'items' with items
  modifier: 'inline'
  itemsNoContent: 'Nothing was published during this month.'
-%}
