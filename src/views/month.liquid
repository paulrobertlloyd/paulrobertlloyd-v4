---
layout: index.liquid
title: Archives
pagination:
  data: archives.months
  size: 1
  alias: archive
  addAllPagesToCollections: true
permalink: "{{ archive.url }}"
eleventyComputed:
  page_title: "{{ archive.data.introduction }}"
  introduction: "{{ archive.data.introduction }}"
---
{%- assign items = collections.publicVisibility | includes: 'date',archive.data.date -%}
{%- render 'items' with items
  itemsNoContent: 'Nothing was published during this month.'
-%}
