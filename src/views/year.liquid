---
layout: index
title: Archive
pagination:
  data: archives.years
  size: 1
  alias: page
  addAllPagesToCollections: true
permalink: "{{ page.url }}"
eleventyComputed:
  changefreq: monthly
  priority: 0.2
  page_title: "{{ page.data.introduction }} · {{ app.name }}"
  introduction: "{{ page.data.introduction }}"
---
{%- assign months = archives.months | includes: 'date',page.fileSlug -%}
{%- render 'section-archives' with months
  id: 'browse',
  title: 'Browse by month'
-%}
{%- assign featured = collections.article | includes: 'data.tags','featured' | includes: 'data.date',page.fileSlug -%}
{%- render 'section' with featured
  id: 'featured'
  title: 'Select articles from this year'
  scrollable: true
  itemsLimit: 3
  itemsGridSize: '15rem'
  itemsComponent: 'card'
-%}
