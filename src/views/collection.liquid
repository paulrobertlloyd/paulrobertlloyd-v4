---
layout: collection
pagination:
  data: archives.collections
  size: 1
  alias: archive
  addAllPagesToCollections: true
permalink: "collections/{{ archive.title | slugify }}/"
tags:
  - collection
eleventyComputed:
  vocabulary: entry
  title: "{{ archive.title }}"
  summary: "{{ archive.summary }}"
  id: "{{ archive.title | slugify }}"
  itemsModifier: "{{ archive.itemsModifier }}"
---
