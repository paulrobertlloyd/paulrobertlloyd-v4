---
layout: index.liquid
title: Events
summary: Exhibitions, conferences and other events I’ve attended.
introduction: Exhibitions, conferences and other events I’ve attended.
pagination:
  data: collections.attended
  size: 48
permalink: "events/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber | plus: 1 }}.html{% else %}/{% endif %}"
---
{% if pagination.pageNumber < 1 and collections.attending.size >= 1 -%}
  {%- render 'section' with collections.attending
    id: 'attending'
    title: 'Attending'
    itemsComponent: 'event'
  -%}
{%- endif %}
{%- render 'section' with pagination.items
  id: 'attended'
  title: 'Attended'
  itemsComponent: 'event'
-%}
{%- render 'pagination' with pagination -%}
