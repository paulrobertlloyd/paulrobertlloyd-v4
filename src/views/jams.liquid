---
layout: index.liquid
title: Jams
pagination:
  data: collections.jam
  size: 36
permalink: "jams/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber | plus: 1 }}.html{% else %}/{% endif %}"
---
{%- render 'items' with pagination.items -%}
{%- render 'pagination' with pagination -%}
