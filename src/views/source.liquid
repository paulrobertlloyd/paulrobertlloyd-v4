---
layout: source
pagination:
  data: sources
  size: 1
  alias: source
  addAllPagesToCollections: true
permalink: "articles/sources/{{ source.title | slugify }}/"
tags:
  - source
sitemap:
  changefreq: monthly
  priority: 0.2
eleventyComputed:
  title: "{{ source.title }}"
  summary: "{{ source.summary }}"
  tag: "source:{{ title | slugify }}"
---
