---
layout: index
title: Writing
summary: Short posts, articles and essays.
introduction: Short posts, articles and essays; mostly written for myself, but sometimes [written for others](/publications/), too.
pagination:
  data: collections.article
  reverse: true
  size: 24
permalink: "articles/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber | plus: 1 }}.html{% else %}/{% endif %}"
---
{%- if pagination.pageNumber < 1 -%}
  {%- assign featured = collections.article | includes: 'data.tags','featured' | reverse -%}
  {%- render 'section' with featured
    id: 'featured'
    title: 'Select articles'
    scrollable: true
    itemsComponent: 'card'
    itemsGridSize: '15rem'
    itemsLimit: 6
  -%}
  {%- render 'section' with pagination.items
    id: 'recent'
    title: 'Recent articles'
    itemsModifier: 'inline'
  -%}
{%- else -%}
  {%- render 'items' with pagination.items
    modifier: 'inline'
  -%}
{%- endif %}
{%- render 'pagination' with pagination -%}
