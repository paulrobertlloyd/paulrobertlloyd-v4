---
layout: skill
pagination:
  data: skills
  size: 1
  alias: skill
  addAllPagesToCollections: true
permalink: "projects/skills/{{ skill.title | slugify }}/"
tags:
  - skill
sitemap:
  changefreq: monthly
  priority: 0.2
eleventyComputed:
  title: "{{ skill.title }}"
  summary: "{{ skill.summary }}"
---
