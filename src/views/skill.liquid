---
layout: skill
pagination:
  data: skills
  size: 1
  alias: skill
  addAllPagesToCollections: true
permalink: "projects/skills/{{ skill.title | slugify }}/"
tags:
  - skill
eleventyComputed:
  title: "{{ skill.title }}"
  summary: "{{ skill.summary }}"
---
