---
layout: skill
pagination:
  data: skills
  size: 1
  alias: skill
  addAllPagesToCollections: true
permalink: "/projects/skills/{{ skill.title | slugify }}/"
tags:
  - skill
eleventyComputed:
  changefreq: monthly
  priority: 0.2
  title: "{{ skill.title }}"
  summary: "{{ skill.summary }}"
---
