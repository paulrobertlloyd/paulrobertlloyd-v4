---
layout: trip
pagination:
  data: trips
  size: 1
  alias: trip
  addAllPagesToCollections: true
permalink: "trips/{{ trip.date | date: '%Y/%m' }}/{{ trip.uid }}/"
tags:
  - trip
eleventyComputed:
  vocab: event
  changefreq: monthly
  priority: 0.2
  title: "{{ trip.title }}"
  summary: "{{ trip.summary }}"
---
